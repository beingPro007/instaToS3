name: Keep Render Alive

on:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl ping
        run: |
          curl https://instatodrive.onrender.com/health
